User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/

# Sitemap URLs are written here automatically when the sitemap is generated
# (Admin -> SEO -> Generate sitemap, or the "sitemap:build" command), using the
# site's configured absolute base URL.
